{
  "name": "@universe/uniswap-nx",
  "version": "0.0.1",
  "private": true,
  "description": "Stub for the internal Nx generators workspace — referenced by the root package.json/bun.lock but not shipped in the public mirror of this repo. Present only so `bun install` succeeds on a public clone.",
  "dependencies": {
    "@nx/devkit": "22.5.4",
    "tslib": "2.3.0"
  }
}
